name: Test

on:
    push:
        branches: ["master"]
    pull_request:

jobs:
    check:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v6
            - uses: dtolnay/rust-toolchain@stable
            - run: cargo check
            - run: cargo test
